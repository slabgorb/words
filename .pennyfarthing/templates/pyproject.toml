# Auto-generated by pennyfarthing init — enables pf CLI via uv run
# To use: uv run --project .pennyfarthing pf <command>
[project]
name = "pennyfarthing-local"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["pennyfarthing-scripts"]

[tool.uv.sources]
pennyfarthing-scripts = { path = "../node_modules/@pennyfarthing/core/pennyfarthing-dist", editable = true }
