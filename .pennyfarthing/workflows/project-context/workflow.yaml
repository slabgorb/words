# generate-project-context Workflow - Migrated from BMAD format
# Generated by migrate-bmad-workflow.mjs

workflow:
  name: generate-project-context
  description: Creates a concise project-context.md file with critical rules and patterns that AI agents must follow when implementing code. Optimized for LLM context efficiency.
  version: "1.0.0"
  type: stepped

  # Step configuration
  steps:
    path: ./steps/
    pattern: step-*.md

  # Variables available in step files
  variables:
    project_root: .
    planning_artifacts: sprint/planning/
    output_file: sprint/planning/generate-project-context.md

  # Agent assignment (customize as needed)
  agent: architect

  # Triggers - when to suggest this workflow
  triggers:
    types: [generate-project-context]
    tags: [generate-project-context, stepped]
